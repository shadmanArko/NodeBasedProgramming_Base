# NodeBasedProgramming_Base
Prepare blocks that can be used to create new objects in the scene (simple shapes like cubes, spheres, cylinders) and that you can use to manipulate them (move, rotate or scale them).  Additionally, create logical blocks that can store/modify values, check/compare them, and then transition to subsequent blocks based on those results.
